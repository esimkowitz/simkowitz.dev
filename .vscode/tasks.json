{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run",
            "type": "shell",
            "command": "cd $(git rev-parse --show-toplevel) && ./setup-local-env.sh && ./run-local.sh",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}